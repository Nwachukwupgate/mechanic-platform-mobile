/**
 * Registered with expo-notifications plugin (garage_ping.wav).
 * Runtime references use the base filename WITHOUT extension (Expo requirement).
 */
export const GARAGE_PING_SOUND = 'garage_ping'

/** New channel IDs — Android channels are immutable; bump when sound/importance changes. */
export const ANDROID_ALERTS_CHANNEL = 'alerts-v2'
export const ANDROID_MESSAGES_CHANNEL = 'messages-v2'
export const ANDROID_BOOKINGS_CHANNEL = 'bookings-v2'
export const ANDROID_DEFAULT_CHANNEL = 'default-v2'
